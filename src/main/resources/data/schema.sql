CREATE TABLE EXPENSE (
                     DIST_ID BIGSERIAL PRIMARY KEY,
                     DIST_NAME VARCHAR(16) NOT NULL,
                     FISCAL_YR BIGSERIAL,
                     ADM INT,
                     DIST_LEV_ADM INT,
                     SCH_LEV_ADM INT,
                     REG_INST INT,
                     CTE INT,
                     SPED INT,
                     SA INT,
                     INST_SUP_SVC INT,
                     PUP_SUP_SVC INT,
                     OP_MAINT INT,
                     TRANS INT,
                     SUB_GF_OP INT,
                     EQUIPMENT INT,
                     LAND_BLD INT,
                     SUB_CAP_EXP INT,
                     TOT_GF_EXP INT,
                     FOOD_SVC INT,
                     COMM_SVC INT,
                     BLD_CONST INT,
                     DEBT_SVC INT,
                     OPEB_SVC INT
);
